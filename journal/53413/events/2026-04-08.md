## 2026-04-08 13:50

- Addressed code review from mbellade on PR #12139: moved the empty-contributors check from interpret(Set, Map) to interpret(Metadata, Map), keeping interpret(Set, Map) unchanged. This is cleaner since the only other caller (SequenceStyleGenerator) always passes a singleton.
- Force-pushed both PRs with the updated code.
- commit: 5c034e1acf - HHH-20321 Schema actions skipped when Metadata has no contributors (7.3)
- commit: 9fc91ededa - HHH-20321 Schema actions skipped when Metadata has no contributors (main)
- PR 7.3: https://github.com/hibernate/hibernate-orm/pull/12138
- PR main: https://github.com/hibernate/hibernate-orm/pull/12139

## 2026-04-08 11:40

- Pushed the ORM fix and created PRs for both branches: PR #12138 targeting 7.3, PR #12139 targeting main (7.4). Branch naming follows Hibernate convention (HHH-20321, HHH-20321-main).
- Added migration-guide.adoc entry under "Changes in Behavior" noting that import.sql files previously ignored (due to no entities) will now be executed. Uses {version} placeholder so it reads correctly on both 7.3 and 7.4.
- Cherry-picked from 7.3 to main, resolved conflict in migration-guide.adoc (different structure on main, dropped 7.3-only sections like Envers and timeout changes).
- Jira issue: https://hibernate.atlassian.net/browse/HHH-20321
- PR 7.3: https://github.com/hibernate/hibernate-orm/pull/12138
- PR main: https://github.com/hibernate/hibernate-orm/pull/12139
- commit: 477e1cf6d7 - HHH-20321 Schema actions skipped when Metadata has no contributors (7.3)
- commit: b38350b369 - HHH-20321 Schema actions skipped when Metadata has no contributors (main)

## 2026-04-08 11:31

- Moved the fix for quarkusio/quarkus#53413 (import.sql not executed with no entities) from the Quarkus workaround into Hibernate ORM itself. The root cause is in SchemaManagementToolCoordinator.ActionGrouping.interpret(Set, Map): when metadata.getContributors() is empty, the loop never executes and all schema actions are skipped. The fix defaults to a synthetic "orm" contributor when the set is empty, reusing the existing loop logic including null-to-NONE defaulting.
- Added hibernate-orm worktree to feature 53413, based on upstream/7.3 (7.3.1-SNAPSHOT) to match Quarkus main which uses 7.3.0.Final.
- Wrote a failing test in SchemaToolingAutoActionTests that builds Metadata with no entities and asserts ActionGrouping.interpret returns the configured create-drop action. All hibernate-core tests pass.
- Reverted the Quarkus workaround in PrevalidatedQuarkusMetadata.getContributors() back to a plain delegation, bumped hibernate-orm.version to 7.3.1-SNAPSHOT, and verified ImportSqlLoadScriptNoEntitiesTestCase still passes.
- Created a Jira issue description for the Hibernate ORM bug tracker.
- commit: f60c3ecc7b - Schema actions skipped when Metadata has no contributors
